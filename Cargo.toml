[workspace]

members = [
    "bin",
    "matching",
    "matching_handlers",
    "merge",
    "parsing",
    "model"
]

[workspace.dependencies]
log = "0.4.20"
uuid = { version = "1.7.0", features = ["v4"] }
